# Novel&Chat 客户端 UI 重新设计 · 全局规划与设计规范

> **本交付物定位**：全局规划 Phase 1。本文档是「墨章 Novel Atelier → Novel&Chat」客户端 UI 重新设计的**顶层设计规范**，面向 UI 设计师与前端工程师。
> 它不替换代码，而是给出可被工程落地的统一标准：设计令牌、组件规范、状态规范、图标规范、关键界面结构与分阶段实施路线。
> 所有决策均承接 `UI_HANDOFF.md` 中已查实的现状与痛点，并补其缺口。

---

## 0. 摘要（一页纸结论）

| 项 | 决策 |
|---|---|
| 对外品牌 | **Novel&Chat**（替换「墨章」「暖阁」全部用户可见标识） |
| 视觉调性 | 保留「暖阁 / Cozy Atelier」暖色调书桌感作为**设计语言名**（style name），但品牌字统一为 Novel&Chat |
| 设计语言 | 暖奶油底色 + 陶土主色 + 玉/金状态色 + 方格稿纸 + 汉字印章 |
| 最高优先级修复 | 三处暗色开发者风组件（TaskQueue / HistoryPanel / ExportDialog）重画回暖阁 + 补挂载 |
| 令牌缺口 | 补 `--danger`、4/8/12/16/24 间距阶梯、阴影阶梯；正式定义 `--ink-900`/`--accent` 或清除 |
| 图标系统 | **汉字印章（品牌/分类）+ 线性 SVG 图标（操作）** 双轨，详见 §6 |
| 交付形式 | 本规范文档（Phase 1）；后续 Phase 2+ 按 §9 路线落地代码 |
| 主题 | 本期仅浅色；§7 预留深色令牌结构，不实现切换 UI |

---

> **📌 主界面已重构（v5.2 终态）**：新主界面（左=纯导航 / 中=纯 AI 对话 / 右=可收起展示空间）已在原型 `NovelChat_main_prototype.html` 实现并逐版评审通过，详见 **§9**。本章 §4.2、§8 的旧三栏结构已被 §9 取代，保留为历史参考。视觉方向由「暖阁暖奶油」调整为 **WorkBuddy 清爽风**（主壳令牌见 §9.1）。

## 1. 品牌与命名统一

### 1.1 决策
- **对外品牌名统一为 `Novel&Chat`**。代码/界面中出现的「墨章」「暖阁 / cozy atelier」一律替换为 Novel&Chat。
- 「暖阁 / Cozy Atelier」降级为**内部设计语言名（style name）**，用于描述暖色书桌调性，不再作为用户可见品牌。

### 1.2 替换落点（供工程师检索替换）
| 位置 | 原值 | 新值 |
|---|---|---|
| `TopBar` 品牌字 | 暖阁·cozy atelier | **Novel&Chat** |
| `LeftRail` / 入口卡印章 | 暖 / 阁 / 书 / 拆 / 续 / 章 / 校 | 保留印章形式，文字改为 N·C / 书 / 拆 / 续 / 架 / 章 / 校（见 §6.3） |
| 窗口标题（`electron/main.js`） | 墨章 · 长篇小说创作工作流 | **Novel&Chat** |
| 产品元数据 / README | 墨章 Novel Atelier | **Novel&Chat** |

### 1.3 品牌标识（Logo）
- 当前**无 logo 图片 / 无 `public/` 目录**。建议新增 `public/` + `build/` 本地资源：
  - 文字标：Novel&Chat（Noto Serif SC 粗，陶土色 `var(--cinnabar)`）。
  - 图形标：一枚「📖 × 对话气泡」融合的线性印章（24px 网格，1.5px 描边），可复用 `.seal` 机制渲染，离线可用。
- 字体离线：将 Noto Serif SC / Noto Sans SC 打包进 Electron（当前靠 Google Fonts 在线拉取，断网即失效）。

---

## 2. 设计原则

1. **本地优先，写作沉浸**：界面是「书桌」不是「控制台」。减少 chrome 干扰，正文区（方格稿纸）必须是视觉重心。
2. **暖而不燥**：奶油底 + 陶土主色，避免纯白/纯黑与高饱和蓝。错误红也走暖砖红，不破坏调性。
3. **印章即语言**：汉字印章作为 Novel&Chat 独有的品牌记忆点，用在分类/入口/书卷标识；功能操作交给线性图标。
4. **令牌唯一真相**：所有颜色/间距/圆角/阴影只走 CSS 变量；禁止散落硬编码与 Tailwind 暗色类。
5. **状态可预期**：空 / 加载 / 错误 / 保存 / HITL 确认，全站一套规范，不各自发挥。
6. **响应式但桌面优先**：以 1280×860 为基准，960×640 为最小可用；<900px 时左右栏隐藏，中区独占。

---

## 3. 设计令牌体系（Design Tokens）

> **使用规则**：本表即 `app/globals.css` `:root` 的目标状态。新增/变更用 ✅新增 / 🔧变更 标注。工程师按「变量名」直接回填，组件禁止再写硬编码色值。

### 3.1 颜色令牌

> ⚠️ **视觉方向修订**：主界面外壳已转向 WorkBuddy 清爽风，主壳权威令牌见 **§9.1**（原型实际取值：`--accent #d97757` 取代 `--cinnabar`、`--jade #3f9d6b`、`--amber #d9a23c`、`--danger #e5484d`）。下表原暖阁令牌**仍用于非主界面的继承面**：方格稿纸底色 `--paper`、汉字印章、以及暗色三件套救火目标（见 §5.2 / §9.8 未决项）。

| 用途 | 变量名 | 值 | 说明 |
|---|---|---|---|
| 页面底色（Chrome） | `--ink` | `#f4ead6` | 暖奶油（命名保留兼容，文档内称「画布底」） |
| 面板最亮面 | `--ink-800` | `#fffdf8` | 气泡/浮层亮面 |
| 面板表面 | `--ink-700` | `#f1e5cf` | 卡片/面板默认底 |
| 悬停面 | `--ink-600` | `#e8dabf` | hover 态 |
| 🔧最深暖调 | `--ink-900` | `#2a2018` | **正式定义**（原未定义，被 AgentPanel/RoleplayChat 引用）；用于极少数深底反白场景 |
| 边框·弱 | `--line` | `rgba(74,54,38,0.10)` | 棕调半透明，保留 |
| 边框·强 | `--line-strong` | `rgba(74,54,38,0.20)` | 保留 |
| 正文文字 | `--fg` | `#3b2f26` | 深棕，保留 |
| 次要文字 | `--fg-dim` | `#7b6a59` | 保留 |
| 更弱文字 | `--fg-faint` | `#a2917d` | 保留 |
| 稿纸底 | `--paper` | `#fbf5e8` | 正文编辑区背景，保留（招牌元素） |
| 稿纸格线 | `--paper-line` | `#e6d9be` | 方格稿纸网格线 |
| 稿纸文字 | `--paper-ink` | `#3a2e24` | 稿纸正文 |
| 主色 陶土 | `--cinnabar` | `#c2673f` | 品牌主色，保留 |
| 主色·亮 | `--cinnabar-bright` | `#d67e52` | hover/强调 |
| 主色·深 | `--cinnabar-deep` | `#a5502c` | 按压/危险提示 |
| 成功 玉 | `--jade` | `#6f9068` | 完成态 |
| 提醒 金 | `--gold` | `#d3a24c` | 草稿/进行中/提醒 |
| ✅错误 砖红 | `--danger` | `#b23b2e` | **新增**，统一错误红（替代散落 `#d44`/`rgba(197,106,63)`） |
| ✅错误·亮 | `--danger-bright` | `#cf5240` | 新增，hover/描边 |
| ✅错误·柔底 | `--danger-soft` | `rgba(178,59,46,0.12)` | 新增，错误条/输入态背景 |
| 🔧品牌强调别名 | `--accent` | `var(--cinnabar)` | **正式定义**（原未定义）；统一指向主色，便于未来换肤 |

> **语义约定（升格为强制）**：`jade = 完成`、`gold = 草稿/进行中`、`cinnabar = 主操作/危险`、`danger = 错误`。任何状态点/标签不得反向使用。

### 3.2 间距令牌（✅新增，4/8/12/16/24 阶梯）
| 变量 | 值 | 典型用途 |
|---|---|---|
| `--space-1` | `4px` | 图标内边距、细间隙 |
| `--space-2` | `8px` | 控件间距、列表项间隙 |
| `--space-3` | `12px` | 卡片内小间距 |
| `--space-4` | `16px` | 面板内边距基准 |
| `--space-5` | `20px` | 区块间距 |
| `--space-6` | `24px` | 大区块/页面留白 |
| `--space-8` | `32px` |  section 间距 |
| `--space-12` | `48px` | 页面级留白 |
| `--space-16` | `64px` | 首页 hero 留白 |

> 工程约束：全面清退内联 `gap:8/12/14/20`、`padding:16/18/22` 等魔法数，统一引用 `--space-*`。

### 3.3 圆角令牌
| 变量 | 值 | 用途 |
|---|---|---|
| `--radius` | `12px` | 面板/卡片 |
| `--radius-sm` | `8px` | 按钮/输入 |
| `--radius-pill` | `999px` | 胶囊标签/印章外圈 |
| `--radius-seal` | `8px` | 印章（大） |
| `--radius-seal-sm` | `6px` | 印章（小） |

### 3.4 阴影令牌（🔧扩展）
| 变量 | 值 | 用途 |
|---|---|---|
| `--shadow-panel` | `0 2px 8px rgba(74,54,38,0.06)` | 面板柔和投影（原仅此一套，保留） |
| `--shadow-sm` | `0 1px 2px rgba(74,54,38,0.06)` | 卡片静态 |
| `--shadow-md` | `0 4px 12px rgba(74,54,38,0.10)` | hover 上浮 |
| `--shadow-float` | `0 8px 24px rgba(74,54,38,0.14)` | 抽屉/模态/弹窗 |

### 3.5 字体与排印令牌
| 变量 | 值 |
|---|---|
| `--font-serif` | `"Noto Serif SC", "Songti SC", "SimSun", serif`（标题/书卷气） |
| `--font-sans` | `"Noto Sans SC", "PingFang SC", "Microsoft YaHei", system-ui, sans-serif`（UI/正文） |
| `--font-mono` | `"JetBrains Mono", ui-monospace, monospace`（diff/工具活动/代码） |

| 阶梯 | 变量 | 值 | 行高 | 用途 |
|---|---|---|---|---|
| xs | `--text-xs` | `12px` | `1.4` | 辅助说明、状态点 |
| sm | `--text-sm` | `13px` | `1.5` | 次要文本、标签 |
| base | `--text-base` | `15px` | `1.6` | UI 正文基准 |
| md | `--text-md` | `17px` | `1.7` | 稿纸正文、阅读 |
| lg | `--text-lg` | `20px` | `1.4` | 区块标题 |
| xl | `--text-xl` | `24px` | `1.3` | 页面标题 |
| 2xl | `--text-2xl` | `30px` | `1.2` | 大标题/hero |
| 3xl | `--text-3xl` | `36px` | `1.15` | 首页主标题 |

> 稿纸正文固定 `17px / 28px 行高 / 衬线`，与方格 28px 对齐（招牌，保留）。

### 3.6 层级令牌（✅新增）
| 变量 | 值 | 用途 |
|---|---|---|
| `--z-panel` | `10` | 栏内浮层 |
| `--z-drawer` | `100` | 折叠抽屉 |
| `--z-modal` | `1000` | 模态/导出弹窗 |
| `--z-toast` | `1100` | 提示条/快照 |

### 3.7 动效令牌（保留并扩展）
- 保留：`--transition-fast 150ms`、`--transition-normal 300ms`、`fadeup`、`pulse`。
- 新增：`shimmer`（骨架屏流光）、`slide-in-right`（右栏抽屉/提案卡进入）。

---

## 4. 布局与外壳规范

### 4.1 窗口画布（硬性约束，不变）
- 默认 `1280×860`，最小 `960×640`，窗口底色 `#faf6f0`。
- 三栏可拖拽：左栏 `248px`（范围 180–420）、右栏 `340px`（范围 280–560），折叠后留 `22px` 细条。
- 设计稿出图基准 **1280×860**；必须验收 **960×640** 最小宽度下左右栏隐藏、中区独占的可用性。

### 4.2 AppShell（三栏外壳）— 旧结构，已被 §9 取代
> 📌 **取代说明**：下方为初始 Phase 1 规划的三栏外壳（LeftRail 5 导航 + 书库 + TaskQueue / 中区路由 / 右栏 AgentPanel 双 Tab）。**新主界面已重构为 §9 的「左=纯导航 / 中=纯 AI 对话 / 右=可收起展示空间」，本章仅保留作历史参考**，新工程以 §9 为准。

- 顶栏 `TopBar`：左 Novel&Chat 品牌字 + 侧栏开关；右 `ProfileSwitcher`（模型接口档）+ 助手开关。
- 左栏 `LeftRail`：导航 5 项（书房首页 / 开新书 / 续写 / 拆书工坊 / 接口设置）+ 书库列表 + 底部 `TaskQueue`（固定高 340px）。
- 中区：路由 `children`。
- 右栏 `AgentPanel`：可折叠为抽屉；「助手 / 角色对话」双 Tab。

### 4.3 通用 UI 材料（升级为正式组件层）
以下语义类在 `globals.css` 已有定义，本期**升格为组件规范**，全站统一调用，禁止各组件重写：
- 按钮：`.btn` / `.btn--primary`（陶土）/ `.btn--ghost`（描边）/ `.btn--sm` / `.btn--danger`（用 `--danger`）。
- 面板：`.panel`（圆角 12 + `--shadow-panel` + `--ink-700` 底）。
- 输入：`.input` / `.textarea` / `.select`（聚焦态 `0 0 0 3px rgba(194,103,63,0.15)`）。
- 标签：`.chip` / `.chip--cinnabar` / `.chip--jade` / `.chip--gold`（胶囊，状态色）。
- 印章：`.seal` / `.seal--sm`（汉字圆形印章，品牌记忆点）。
- 状态点：`.dot` / `.dot--done`(jade) / `.dot--draft`(gold) / `.dot--active`(cinnabar)。
- 卡片：`.entry-card` / `.work-card`（hover 上浮 + 暖光）。

---

## 5. 组件重做规范

> ⚠️ 两大类问题必须在本期解决：**(A) 暗色三件套重画回暖阁 + 补挂载**；**(B) 清退内联样式与 Tailwind 暗色类**，统一走令牌 + 语义类。

### 5.1 品牌与导航（TopBar / LeftRail / ProfileSwitcher）
- 品牌字改 **Novel&Chat**（见 §1.2）。
- 导航项图标：改用 §6 线性图标；书库项前加状态点（jade=在写 / gold=搭骨架 / faint=酝酿）。
- 其余结构保留。

### 5.2 ⚠️ 暗色三件套 · 重画回暖阁（最高优先级）

**`TaskQueue.tsx`（左栏底部，常驻高频）**
- 移除 `text-neutral-*` / `bg-neutral-900` / `bg-blue-700` / `bg-amber-*` / `text-red-400` 全部 Tailwind 暗色类。
- 重画为暖阁面板：底 `--ink-700`、边框 `--line`、状态用 chip（`等待中`gold / `执行中`cinnabar / `已完成`jade / `失败`danger / `已暂停`faint）。
- 进度条：轨道 `--ink-600`，填充 `--cinnabar`，完成段 `--jade`。
- 操作按钮：`.btn--sm` + 线性图标（开始/暂停/重试/删除）。

**`ExportDialog.tsx`（导出弹窗，正式功能入口）**
- 当前未挂载 → 接到工作台顶部「导出」按钮（Phase 2 落地）。
- 重画：模态用 `.panel` + `--shadow-float`，遮罩 `rgba(42,32,24,0.35)`；格式/范围/选项用 `.chip` 多选 + `.input`/`.select`；确认 `.btn--primary`，取消 `.btn--ghost`。
- 错误态用 `--danger-soft` 错误条，禁原生 `alert`。

**`HistoryPanel.tsx`（版本历史 + diff + 回滚）**
- 当前未挂载 → 接到章节级「版本历史」入口（Phase 2 落地）。
- 重画：快照列表 `.entry-card`；diff 用 `--font-mono` + 增行 `--jade-soft` / 删行 `--danger-soft`（新增 `--jade-soft rgba(111,144,104,0.14)`）；回滚 `.btn--sm`。

### 5.3 工作台核心（保留功能，规范样式）
- `Workspace`：书名编辑、两步进度切换（`[1 立意·大纲]—[2 铺陈·正文]`）、进度条（字数/章数）、`保存中…/已保存`状态点（gold pulse→jade check）。
- `StepOutline`（1039 行 ⚠️）：功能保留；已拆出 `step-outline/*` 子组件，**作范本**。本期仅规范其样式走令牌，不强行拆分。
- `StepWriting`（1085 行 ⚠️）：功能保留；**建议拆出**「章节导航 / 稿纸编辑 / 设定库 / 伏笔 / 提示词」子模块（Phase 4），本期先规范样式。
- `CodexPanel` / `PromptLibrary` / `ChangeSummary`：保留，样式统一令牌。

### 5.4 创作助手（保留功能性槽位）
- `AgentPanel` / `AgentChat` / `RoleplayChat`：清退内联 `style={{}}`，改用语义类 + 令牌。
- **必须保留的槽位**：消息流气泡（用户右/陶土，助手左/奶白 `--ink-800`）、工具活动 chip（gold=调用中 / jade=完成）、**HITL 写操作提案卡（diff + 确认写入/取消）**、流式光标、技能选择器 `SkillPicker`。
- 选角 → 对话的两态（角色来自本书「人物」设定）保留。

### 5.5 拆书工坊 / 书架 / 设置 / 续写
- `CardLibrary` / `ArchiveResult`：保留，样式令牌化。
- 书架 `work-card` 网格按阶段标签（酝酿/搭骨架/在写）显示状态点。
- 设置页 `ProfileSwitcher` 多套 API 配置卡片化，测试连接按钮 `.btn--primary`。

---

## 6. 图标系统规范（我的建议：双轨制）

> 当前项目**无任何图标库**，只有汉字印章 + 少量 Unicode。建议采用**双轨**：印章管「品牌/分类」，线性图标管「操作」。

### 6.1 轨道一 · 汉字印章 `.seal`（保留并扩展）
- 角色：Novel&Chat 独有品牌记忆点，用于**入口卡 / 分类标识 / 书卷标识**。
- 文字集：`N·C`（品牌标）、`书`（开新书）、`拆`（拆书坊）、`续`（续写）、`架`（书架）、`章`（章节）、`校`（校对/导出）、`角`（角色对话）。
- 样式：圆形/方圆形，陶土底白字或描边，尺寸 40/28（`.seal` / `.seal--sm`），圆角 `--radius-seal`。

### 6.2 轨道二 · 线性 SVG 图标（✅新增）
- 风格：1.5px 描边、圆角端帽、24px 网格、`fill:none`、`stroke:currentColor`（跟随 `--fg` / `--cinnabar`）。
- 引入方式：建议用 `lucide-react`（轻量线性，风格一致）或自建 SVG 雪碧图；离线打包进 Electron。
- 常用集：设置⚙、搜索🔍、加＋、删🗑、编辑✎、刷新↻、展开/折叠▸、上传⬆、下载/导出⬇、菜单☰、关闭✕、对勾✓、警告⚠、信息ℹ、书📖、对话💬、角色👤、保存💾、暂停⏸、播放▶。

### 6.3 搭配规则（强制）
1. **同行不混风格**：入口卡用印章，则其内部操作按钮用线性图标，不出现两种「图标语言」并排争抢。
2. **印章=名词/分类，线性图标=动词/操作**。
3. 列表项操作（删/改/刷新）一律线性图标 + `--fg-dim`，hover 变 `--cinnabar`。
4. 品牌标 `N·C` 仅在 TopBar 与登录/关于等品牌场景出现，不做功能按钮。

---

## 7. 状态规范（空 / 加载 / 错误 / 保存 / HITL）

> 当前空态零散、加载多为文字、错误散落（chip 当错误条 / `#d44` / 原生 alert）。本期建立全站一套。

### 7.1 空状态（Empty）
- 结构：居中印章/线性图标（faint）+ 一句引导文案（`--fg-dim`）+ 一个主操作 `.btn--primary`。
- 文案风格：「这里还空空的，先开一本新书吧～」式轻松语气，契合暖阁调性。
- 适用：书架空、书库空、角色空、对话空、卡库空。

### 7.2 加载态（Loading）
- 禁止纯文字「加载中…」做主区加载；改用**骨架屏 `Skeleton`**（shimmer 动效，`--ink-600` 流光）。
- 行内小加载：按钮内 spinner（陶土）或状态点 pulse。
- 流式生成（`StreamingPanel`）：保留占位 + 进度，用 gold pulse。

### 7.3 错误态（Error）✅统一用 `--danger`
- 错误条组件 `ErrorNote`（扩展）：左 `--danger-bright` 图标 + `--fg` 文案 + 可选「重试」`.btn--sm`，底 `--danger-soft`，圆角 `--radius-sm`。
- 输入校验失败：输入框边框 `--danger` + 下方 `--danger` 小字提示。
- 禁止原生 `alert`；禁止用 `chip--cinnabar` 当错误条（cinnabar=主操作，语义冲突）。

### 7.4 保存态（Save）
- 工作台 `保存中…`（gold pulse 点）→ `已保存`（jade check），900ms 防抖，保留。

### 7.5 HITL 提案确认卡（保留槽位）
- 结构：标题「待确认改动」+ diff 视图（`--font-mono`，增 jade-soft / 删 danger-soft）+ `[确认写入]`(`.btn--primary`) `[取消]`(`.btn--ghost`)。
- 位置：右栏对话流内，slide-in-right 进入。

### 7.6 深色主题（预留，不实现）
- `:root` 之外新增 `[data-theme="dark"]` 变量块草案（奶油→深棕、面板→近黑暖灰、文字反白），结构到位但本期不接切换按钮/逻辑。未来换肤只需补 UI。

---

## 8. 关键界面高保真结构（历史参考 · 部分已被 §9 取代）

> ⚠️ **取代说明**：新主界面的「书房首页 / 作品工作台 / 右栏 AgentPanel 双 Tab」已被 **§9** 重构（左=纯导航 / 中=纯 AI 对话 / 右=可收起展示空间）。下方 ①~③ 为旧结构，保留参考；④~⑦（拆书工坊 / 设置 / 导出 / 任务队列）作为子能力仍有效，经中栏「+」展开器或对话内模态触达（见 §9.4）。窗口基准 1280×860。

**① 书房首页 `/`**（hero 文案 + 四入口卡：开新书/拆书坊/续写/书架，hover 上浮+暖光；未配置接口时顶部 `--gold` 提醒条）

**② 作品工作台 `/project/[id]`**
- 顶：`← 书房` + 《书名》[改名] + 题材 chip + `保存中…/已保存`。
- 切换：`[1 立意·大纲]———[2 铺陈·正文]` + 进度条（字数/章数）。
- Step1 大纲：左设定编辑（创作设定/故事圣经/分卷脉络 Tab）｜右卷·章结构（VolumeCard/ChapterRow）。
- Step2 正文：左章导航（状态点）｜右 `[正文|设定库|伏笔]` Tab + 方格稿纸（衬线 17px/28px）。

**③ 右栏 AgentPanel**：`[助手][角色对话]` + 折叠 ✕；助手 Tab（气泡流 + 工具 chip + HITL 卡 + 技能/停止/发送）；角色 Tab（选角→对话）。

**④ 拆书工坊 `/style`**：`[学文风|拆设定]` 双 Tab + 上传/分析 + 文风七维卡 + 作品档案 + 卡库。

**⑤ 设置 `/settings`**：多套 API 配置卡（名称/BaseURL/Key/模型/温度）+ 测试连接。

**⑥ 导出弹窗 `ExportDialog`**：格式（EPUB/MD/TXT）chip + 范围 + 选项 + 确认/取消（重画后）。

**⑦ 任务队列 `TaskQueue`**：左栏底部常驻，状态 chip + 进度条 + 操作（重画后暖阁风）。

---

## 9. 新主界面架构（v5.2 终态 · 承接可交互原型）

> **状态**：本主界面已在 `NovelChat_main_prototype.html`（v5.2 单文件可交互原型）实现并逐版评审通过，功能基本跑通。本章是该原型的设计定稿，**取代** §4.2 中「LeftRail 5 导航 + 书库 + TaskQueue」与 §8 中「①书房首页 / ②作品工作台 / ③右栏 AgentPanel 双 Tab」的旧结构。旧结构保留为参考，新工程以本章为准。
> 配套资源：原型为活的参照；本章所有尺寸 / 类名 / 交互均可在原型中核到。窗口基准 1280×860。

### 9.1 视觉方向修订：暖阁暖奶油 → WorkBuddy 清爽风
- 主界面整体从原「暖阁暖奶油书桌感」调整为 **WorkBuddy 清爽风**：浅色近白画布、细边框弱阴影、弱暖调、克制珊瑚色点缀。
- **唯一保留的暖调记忆点是小说阅读器**：方格稿纸仍用衬线（Noto Serif SC）+ 暖白底 `--paper`，保留「书卷感」，与新壳清爽形成对比焦点。
- 主壳采用下方「清爽风令牌」（取自原型 `:root`，为新的主界面权威令牌层）；§3.1 原暖阁令牌仅用于非主界面的继承面（印章、方格稿纸底色、暗色三件套救火目标）。

**主壳清爽风令牌（原型实际取值）**
| 用途 | 变量 | 值 |
|---|---|---|
| 画布底 | `--bg` | `#f6f7f9` |
| 表面 / 浮层亮面 | `--surface` | `#ffffff` |
| 表面二级 | `--surface-2` | `#f1f3f5` |
| 表面三级 | `--surface-3` | `#e9edf0` |
| 边框·弱 | `--border` | `rgba(27,31,36,0.10)` |
| 边框·强 | `--border-strong` | `rgba(27,31,36,0.16)` |
| 正文 | `--fg` | `#1f2328` |
| 次要 | `--fg-dim` | `#656d76` |
| 更弱 | `--fg-faint` | `#8b949e` |
| ✅ 品牌强调（克制珊瑚） | `--accent` | `#d97757` |
| 强调·深 | `--accent-strong` | `#b85c3c` |
| 强调·柔底 | `--accent-soft` | `rgba(217,119,87,0.12)` |
| 完成 玉（清新绿） | `--jade` | `#3f9d6b` |
| 草稿 / 进行中 金 | `--amber` | `#d9a23c` |
| 错误 砖红 | `--danger` | `#e5484d` |
| 稿纸底（保留暖白） | `--paper` | `#fbfaf6` |
| 稿纸格线（中性） | `--paper-line` | `rgba(27,31,36,0.06)` |
| 圆角 | `--radius` / `--radius-sm` / `--radius-pill` | `10px` / `8px` / `999px` |
| 阴影 | `--shadow-sm` / `--shadow-md` | `0 1px 3px rgba(27,31,36,.06)` / `0 6px 20px rgba(27,31,36,.10)` |
| 字体 | `--font-ui` / `--font-serif` | 系统+PingFang SC/雅黑(sans) / Noto Serif SC(稿纸·书卷) |

> **与原 §3.1 的映射**：`--accent`(#d97757) 取代原 `--cinnabar`(#c2673f) 作主强调；`--jade`(#3f9d6b) 取代原 `#6f9068`；`--amber`(#d9a23c) 取代原 `--gold`(#d3a24c)；`--danger`(#e5484d) 取代原 `#b23b2e`。语义不变：`jade=完成`、`amber=草稿/进行中`、`accent=主操作`、`danger=错误`。

### 9.2 三栏外壳布局
- 外壳 `.app`（flex 列）：`topbar` 52px + 下方 `.left / .center / .right` 横向三栏。
- 尺寸与收起：

| 栏 | 展开宽 | 收起 | 收起形态 |
|---|---|---|---|
| TopBar | 52px | — | 左 品牌字+侧栏开关；右 「新对话」重置 |
| 左 `.left` | 248px | 56px | **图标轨**（nav 收起，仅显图标按钮） |
| 中 `.center` | `flex:1`（最大空间） | 自动吃满 | 纯 AI 对话 |
| 右 `.right` | 380px | `width:0` | **浮动手柄 tab**（贴屏幕右缘，不占布局宽度） |

- 收起让位：左 / 右任一收起，中栏 `flex:1` 自动扩张吃满让出的空间（v4.2 / v4.5 已修「收起留空白」问题）。
- 右栏收起后 `.right.collapsed{width:0;overflow:visible}`，`.right-rail` 绝对定位浮于屏幕右缘作重开 tab；展开后 `display:none`。

### 9.3 左栏：可收起纯导航
- 结构 `left-content`：① `nav-list` 仅一项 **酒馆AI**（`navTavern`→`openTavern`）；② `section-label 书架` + 三张 `book-card`（长夜行 / 山海拾遗 / 春山旧梦，选中 `active` 高亮，状态点 jade=在写 / gold=搭骨架 / faint=酝酿）。
- 收起 → `left-rail` 图标轨：酒馆AI 按钮 + 分隔 + 书架按钮，点图标 `expandLeft()` 展开。
- **已删除**：原「资源 / 大纲 / 稿纸」三类子模块、「书房首页」、「角色对话」入口（v3 / v4 决策）。

### 9.4 中栏：纯 AI 对话（最大空间，且只能是对话系统）
> v4 决策：工作区**只能是 AI 对话**，删除工作台首页 hub 与「角色对话」卡。

- **顶部快速创作栏** `create-bar`：四个 `create-chip`（世界观 / 人物设定 / 大纲 / 章节）→ 触发 AI 走「读取设定 → 生成中 → 产出可编辑 .md 草稿」（加强对话框工作强度）。
- **消息流**：用户右（accent-soft 气泡）/ 助手左（白气泡 + accent 渐变头像）；**工具活动 chip**（amber=调用中 → jade=完成）；流式光标。
- **HITL 提案卡（核心槽位，保留）**：`hitl-card` 含 文件名 + 类型徽标（accent=草稿 / jade=已写入）+ **可编辑 .md 文本框**（`md-editor`，确认前可改）+ 操作（重新生成 / 取消 / **确认写入**）。
- **composer「+」展开器**（仿 WorkBuddy 对话窗）：圆形 `plus-btn` → 展开 `plus-panel`，藏五个能力：
  - 创作类（开一本新书 / 续写 / 拆书工坊）→ `seedChat` 进对话；
  - 接口设置 / 技能 → 弹出**对话内模态** `modal-overlay`（不离开对话界面，关掉回聊天，保证中间恒为对话）。
- 顶栏「新对话」按钮：`resetChat` 清空重置（替代旧「返回首页」）。

### 9.5 右栏：可收起展示空间（常态空白收起）
- 常态 `.right.collapsed`（空白不展开）；仅点左栏 酒馆AI 或 书架某书 才展开对应页。
- **两个完全独立的详情页**（v5.2 终态，互不嵌套、不在同一页面中）：
  - `#page-book`：点书架某书 → `.right.book #page-book{display:flex}`。
  - `#page-tavern`：点左栏 酒馆AI → `.right.tavern #page-tavern{display:flex}`。
  - 两者 CSS 互斥显示，**永不同时存在**；各自带独立 `.detail-head`（标题 + 分段 + 收起）与 `.detail-content`，数据 / DOM / 渲染函数三处都不交叉。
- **每页三分段** `[成果 | 阅读 | 文档]`（`switchSeg(page,seg)` 只操作本页内 pane）：
  - **成果**：卡片组 = 一句话立意(logline) / 梗概 / 章节列表(状态点) / 核心设定(meta-tags)。
  - **阅读**：方格稿纸衬线阅读器（`reader`：卷·章头 + 衬线正文）；确认写入章节后落稿。
  - **文档**：`.md` 文件列表（`doc-list`）+ Markdown 渲染阅读器（`doc-reader`：标题 + 类型徽标 + 轻量 md→html 渲染）。
- **收起**：每页 `.detail-head` 的 chevron → `toggleRight()` → `.right` 加 `collapsed`（width:0），右缘浮动手柄 tab 可重开（酒馆AI / 书架 两按钮）。

### 9.6 事实源变更：设定类产物以 .md 保存
- **架构级决策**（v2 起）：世界观 / 人物设定 / 大纲 等以 **`.md` 文件** 为事实源，替代原 `data/` 的 JSON 产物。
- AI 在对话中产出**可编辑 .md 提案卡**，确认后落盘：章节类 → 右栏阅读器落方格稿纸；设定类 → 右栏文档列表 + 可重开 Markdown 渲染。
- 原型用假数据跑通体验（`docsStore` 预置 世界观.md / 人物设定_沈寄.md；`docsStoreTv` 预置 灵感碎片_长夜行.md）。**真写代码时需处理 JSON→.md 迁移 / 共存**（原 `data/` 21 个 JSON）。

### 9.7 交互闭环（确认写入 → 右栏落稿）
1. 中栏「快速创作」/「+」→ AI 产出 可编辑 .md 提案卡。
2. 用户在卡内改完 → 点「确认写入」。
3. 章节类：`switchSeg('book'/'tavern','reader')` + `fillReaderFromMd()` → 右栏方格稿纸落稿。
4. 设定类：`switchSeg(...,'doc')` + `openDoc(name)` → 右栏文档列表重开渲染。
5. 收起右栏 → 空间让给中栏；点右缘手柄 / 左栏入口可再展开。

### 9.8 与原 §4.2 / §8 的关系
- **取代**：§4.2 的「LeftRail 5 导航 + 书库 + TaskQueue 常驻」、§8 的「①书房首页 / ②作品工作台 / ③右栏 AgentPanel 助手·角色对话双 Tab」。
- **保留仍有效**：拆书工坊 / 接口设置 / 技能 / 导出 / 任务队列 作为子能力存在——前三者经中栏「+」展开器或对话内模态触达；导出 / 任务队列沿用 §5.2 的救火规范（接工作台顶部 / 章节级入口）。方格稿纸、汉字印章、HITL 提案卡槽位全部保留。
- **未决（待主理人确认）**：暗色三件套（TaskQueue / HistoryPanel / ExportDialog）救火目标原为「回暖阁」（§5.2），而新主壳已转 清爽风；二者是否统一为清爽风令牌，需回头对齐。

---

## 10. 实施路线（分阶段，代码落点）

| Phase | 范围 | 落点 | 验收 |
|---|---|---|---|
| **P1 规划（本期）** | 本规范文档 | `NovelChat_UI_REDESIGN_SPEC.md` | 评审通过 |
| **P2 令牌地基** | 补 `--danger`/`--space-*`/`--shadow-*`；定义/清 `--ink-900`/`--accent`；品牌字替换 | `app/globals.css` `:root` + 全站检索替换 | 无硬编码色、无 undefined 变量 |
| **P3 救火暗色三件套** | TaskQueue/HistoryPanel/ExportDialog 重画回暖阁 + 补挂载（导出接工作台、历史接章节） | 对应 `.tsx` + `Workspace.tsx` 挂载 | 全站无 Tailwind 暗色类 |
| **P4 样式统一** | 清退内联 style；通用 UI 升格组件；StepWriting 拆分 | 各组件 + `globals.css` | 4 类混杂→令牌单源 |
| **P5 状态规范** | 空/加载骨架/错误条/保存/HITL 统一 | 新增 `Skeleton`/`ErrorNote` 组件 | 全站状态一致 |
| **P6 图标接入** | 印章集 + 线性图标库 + 搭配规则 | `public/` + 组件 | 双轨落地、离线可用 |
| **P7 预留深色** | `[data-theme="dark"]` 令牌块（不接 UI） | `globals.css` | 结构就绪 |

> 📌 **主界面原型已就绪**：新主界面（§9）的可交互评审原型为 `NovelChat_main_prototype.html`（v5.2），功能基本跑通，是 §9 的活的参照与后续 Phase 2+ 落代码的基线。P3–P6 不再需要从零出高保真切片。

---

## 11. 设计师 / 工程师协作清单

- [ ] 工程师：按 §3 令牌表回填 `globals.css`，并全局检索清除硬编码色值与 `--ink-900`/`--accent` 未定义引用。
- [ ] 设计师：P1 评审后产出 P3–P6 所需高保真切片（可交互 HTML 原型，下一期交付物）。
- [ ] 双方：确认 Novel&Chat 品牌字在所有落点替换完毕（§1.2）。
- [ ] 工程师：将 Noto 字体与图标资源打包进 Electron，确保离线可用。
- [ ] 设计师：P5 状态组件定稿后，补一套浅色主题下的组件库截图基线。

---

*本规范承接 `UI_HANDOFF.md`（实地查阅 `components/`、`app/`、`lib/`、`electron/`、`app/globals.css`）。品牌决策：对外统一 Novel&Chat。Phase 1 交付，后续按 §9 落地。*
