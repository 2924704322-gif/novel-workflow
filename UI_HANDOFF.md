# 墨章 Novel Atelier — UI 改版交接文档

> 本文档面向 **UI 设计师（非技术读者）**。前半部分用大白话讲清楚产品是什么、界面长什么样、有哪些设计材料可复用；后半部分给出设计师可直接照做的清单与约束。
> 所有信息均来自对当前代码的实地查阅（`components/`、`app/`、`lib/`、`electron/`、`app/globals.css`），不是凭空猜测。

---

## 1. 产品一句话定位 + 核心使用场景

**墨章 Novel Atelier 是一个「本地优先」的长篇小说 AI 创作桌面软件**（Windows / macOS，通过 Electron 打包成单文件 exe）。它服务于一心想写长篇（几十万到上百万字）的小说作者，帮他们在 AI 辅助下完成「从一句话灵感到大部头成书」的全过程，同时保证数据留在自己电脑上。

**核心人物**：长篇小说作者（网文 / 严肃文学皆可）。他们要的不是聊天机器人，而是一个「写作工作台」——能拆书学别人文风、能搭大纲、能一章一章地把正文写出来、能和角色对话找灵感、最后导出成电子书。

**两段式写作心法（贯穿全产品的主线）**：
1. **第一步「立意 · 大纲」**：先把整本书的骨架立起来（故事圣经 / 分卷脉络 / 章节脉络）。
2. **第二步「铺陈 · 正文」**：再一章一章地把正文写出来，正文区是「方格稿纸」样式。

**典型使用路径**：
`书房首页` → `开一本新书 / 拆书工坊 / 续写` → `作品工作台（大纲 → 正文）` → 写作时随时打开右侧 `助手 / 角色对话` → 写完 `导出 EPUB / MD / TXT`。

---

## 2. 页面 / 路由地图（Screen Map）

所有可见页面如下。带 ⭐ 的是主界面，带 🗂 的是普通整页，带 🧩 的是嵌入在其它界面里的区块/浮层。

| 路由路径 | 页面功能 | 主要 UI 区块 | 类型 |
|---|---|---|---|
| `/` | 书房首页 | Hero 欢迎语 + 三张入口卡（开新书 / 拆书工坊 / 续写 / 书架）+ 未配置接口提示 | 🗂 整页 |
| `/new` | 开一本新书 | 书名输入框 + 创建按钮 | 🗂 整页（极简表单） |
| `/continue` | 续写一本书 | 上传 .txt → 解析预览（卷/章树 + 正文阅读器）→ 套用文风卡/设定卡 → 导入 | 🗂 整页（多步向导） |
| `/style` | 拆书工坊 | 「学文风 / 拆设定」双 Tab；上传/分析；文风七维卡片；作品档案；卡库列表 | 🗂 整页（最复杂的内容页之一） |
| `/shelf` | 我的书架 | 作品卡片网格（按阶段标签：酝酿/搭骨架/在写）+ 删除 | 🗂 整页 |
| `/settings` | 模型接口设置 | 多套 API 配置管理（名称/BaseURL/Key/模型/温度）+ 测试连接 | 🗂 整页 |
| `/project/[id]` | **作品工作台（主界面）** ⭐ | 顶部书名+进度条+两步切换；下方切换 `StepOutline`（大纲）或 `StepWriting`（正文） | ⭐ 主界面（被 AppShell 三栏包裹） |
| `/agent` | Agent 对话开发预览页 | 左侧说明 + 右侧 `AgentChat`（独立预览，内部开发用） | 🗂 内部页（非正式功能入口） |
| （全局）`AppShell` | 三栏应用外壳 | 顶栏 TopBar + 左资源栏 LeftRail + 中工作区 + 右创作助手 AgentPanel | 🧩 外壳（所有页面共享） |
| （左栏底）`TaskQueue` | 批量任务队列 | 任务列表 + 新建批量任务表单 + 进度/操作 | 🧩 嵌入左栏底部 |
| （右栏）`AgentPanel` | 创作助手面板 | 「助手 / 角色对话」双 Tab 容器 | 🧩 嵌入右栏（可折叠为抽屉） |
| `ExportDialog` | 导出作品弹窗 | 格式/范围/选项 | 🧩 模态弹窗（**当前未在页面挂载，见 §7**） |
| `HistoryPanel` | 版本历史 + diff + 回滚 | 快照列表 + 差异视图 + 回滚 | 🧩 面板组件（**当前未在页面挂载，见 §7**） |

> **设计师须知**：产品是桌面应用，没有浏览器地址栏。窗口由 Electron 控制（见 §5）。`/agent` 是开发者调试页，可忽略或改造成正式功能。

---

## 3. 组件清单（Component Inventory）

按业务模块分组。行数来自实际统计；标「⚠️超大」的组件逻辑与 UI 全挤在一个文件里，强烈建议拆分。

### 3.1 应用外壳 / 导航
| 组件 | 文件路径 | 职责一句话 | 行数 | 复用程度 | 备注 |
|---|---|---|---|---|---|
| AppShell | `components/AppShell.tsx` | 全局三栏外壳：顶栏 + 左栏 + 中区 + 右栏，支持拖拽调宽、折叠、状态记忆 | 184 | 全局唯一 | 左栏 180–420px、右栏 280–560px 可调 |
| TopBar | `components/TopBar.tsx` | 全宽顶栏：品牌「暖阁」+ 侧栏开关 + 模型档切换 | 42 | 全局唯一 | 由 AppShell 渲染一次 |
| LeftRail | `components/LeftRail.tsx` | 左资源栏：导航项 + 书库列表 + 底部任务队列 | 133 | 全局唯一 | 导航 5 项：书房/新书/续写/拆书/设置 |
| ProfileSwitcher | `components/ProfileSwitcher.tsx` | 顶栏右侧的「模型接口档」下拉切换 | 65 | 顶栏内 | 切换当前生效的 API 配置 |

### 3.2 作品工作台（主界面核心）
| 组件 | 文件路径 | 职责一句话 | 行数 | 复用程度 | 备注 |
|---|---|---|---|---|---|
| Workspace | `components/Workspace.tsx` | 作品工作台容器：加载项目、书名编辑、两步进度切换、自动保存 | 299 | 仅 `/project/[id]` | 内含 StepTab 子组件 |
| StepOutline | `components/StepOutline.tsx` | **第一步「立意·大纲」**：创作设定 → 故事圣经 → 分卷脉络，三步向导 | 1039 | 仅工作台 Step1 | ⚠️超大，建议拆 |
| StepWriting | `components/StepWriting.tsx` | **第二步「铺陈·正文」**：章节导航 + 方格稿纸正文 + 设定库/伏笔/提示词 Tab | 1085 | 仅工作台 Step2 | ⚠️超大，建议拆 |
| CodexPanel | `components/CodexPanel.tsx` | 设定库面板 + 伏笔面板（导出 `CodexPanel`、`ForeshadowPanel`） | 379 | 仅 StepWriting | 含人物/地点/势力/伏笔编辑 |
| ChangeSummary | `components/ChangeSummary.tsx` | 重新生成后「一致性统一」结果提示卡（哪些被改、哪些章节待复核） | 118 | StepOutline + StepWriting 共用 | 同一组件两处复用 ✓ |
| PromptLibrary | `components/PromptLibrary.tsx` | 提示词库面板（写作偏好/调整方向，标来源、可启停） | 154 | 仅 StepWriting | 导出 `PromptLibraryPanel` |

### 3.3 大纲子组件（`components/step-outline/`，由 StepOutline 组合）
| 组件 | 文件路径 | 职责一句话 | 行数 | 备注 |
|---|---|---|---|---|
| StepNav | `step-outline/StepNav.tsx` | 三步进度导航（创作设定/故事设定集/分卷脉络） | 93 | 带完成态 ✓ |
| BibleView | `step-outline/BibleView.tsx` | 故事圣经（logline/梗概/世界观/主题/情调）编辑 | 126 | |
| VolumeCard | `step-outline/VolumeCard.tsx` | 单卷卡片：卷名/摘要/计划章数/展开章节脉络/删除 | 228 | 含 ChapterRow 列表 |
| ChapterRow | `step-outline/ChapterRow.tsx` | 单章行：标题/脉络编辑 + 重生成/上下移/删除 | 114 | |
| RegenDialog | `step-outline/RegenDialog.tsx` | 「带方向重新生成」的弹窗表单 | 64 | |
| StreamingPanel | `step-outline/StreamingPanel.tsx` | 流式生成时的占位/进度展示 | 23 | |
| EditableRow | `step-outline/EditableRow.tsx` | 通用可编辑行（标签+输入） | 25 | 小工具 |
| Field | `step-outline/Field.tsx` | 通用字段包装（标签+子元素） | 10 | 小工具 |
| ErrorNote | `step-outline/ErrorNote.tsx` | 错误提示小条 | 17 | 小工具 |

### 3.4 写作助手 / 对话
| 组件 | 文件路径 | 职责一句话 | 行数 | 复用程度 | 备注 |
|---|---|---|---|---|---|
| AgentPanel | `components/AgentPanel.tsx` | 右栏容器，切「助手 / 角色对话」双 Tab | 108 | 全局唯一 | 从 URL 推断当前作品 id |
| AgentChat | `components/AgentChat.tsx` | 自然语言写作助手：消息流 + 工具活动 + 写操作提案确认(HITL) + 输入框 | 419 | 右栏 + `/agent` | 依赖 `lib/agent/useChat` 流式 |
| RoleplayChat | `components/RoleplayChat.tsx` | 沉浸式角色对话：选角 → 1v1/多角色轮转对话 | 608 | 仅右栏 Tab | 依赖 `lib/roleplay/useRoleplay` |
| SkillPicker | `components/SkillPicker.tsx` | 技能选择面板（写作/规划/维护分组，带参数表单） | 202 | 仅 AgentChat | 由 AgentChat 调用 |

### 3.5 拆书工坊
| 组件 | 文件路径 | 职责一句话 | 行数 | 复用程度 | 备注 |
|---|---|---|---|---|---|
| CardLibrary | `components/CardLibrary.tsx` | 已保存「文风卡 / 设定卡」列表，可调用/删除 | 104 | 仅 `/style` | 数据形态无关的通用列表 |
| ArchiveResult | `components/ArchiveResult.tsx` | 作品档案展示（剧情/世界观/人物/主线）+ 二创开新书 | 215 | 仅 `/style` | |

### 3.6 任务队列 / 版本历史 / 导出
| 组件 | 文件路径 | 职责一句话 | 行数 | 复用程度 | 备注 |
|---|---|---|---|---|---|
| TaskQueue | `components/TaskQueue.tsx` | 批量任务队列：新建（批量续写/摘要/全卷流水线）+ 进度 + 启停/重试/删 | 424 | 左栏底部 | ⚠️样式与其它界面严重不一致（见 §7） |
| HistoryPanel | `components/HistoryPanel.tsx` | 版本历史快照 + diff + 回滚 | 176 | **未挂载** | 见 §7（疑似预留/弃用） |
| ExportDialog | `components/ExportDialog.tsx` | 导出格式/范围/选项弹窗 | 183 | **未挂载** | 见 §7（疑似预留/弃用） |

> **通用 UI 材料**：按钮（`.btn` / `.btn--primary` / `.btn--ghost` / `.btn--sm` / `.btn--danger`）、面板（`.panel`）、输入框（`.input` / `.textarea` / `.select`）、标签（`.chip` / `.chip--cinnabar` / `.chip--jade`）、印章徽标（`.seal` / `.seal--sm`）、状态点（`.dot` / `.dot--done` / `.dot--draft`）、卡片（`.entry-card` / `.work-card`）。这些都在 `app/globals.css` 里定义，是现有最成体系的「组件层」。

---

## 4. 当前设计系统（Design Tokens）

> **重要结论**：项目**已有基础设计令牌**，但它们写在 `app/globals.css` 的 CSS 变量里（不是 Tailwind 主题、也不是 Figma Token）。当前**没有统一沉淀成设计师可一键复用的 Token 表**——这正是设计师建立规范的最佳切入点。

### 4.1 设计语言定位
- 名称：**「暖阁 / Cozy Atelier」**（暖色调、奶油书桌、晨光感）。
- 主色（陶土 terracotta）：`--cinnabar #c2673f`（主操作/品牌）；亮 `#d67e52`、深 `#a5502c`。
- 辅色（蜂蜜金 honey-gold）：`--gold #d3a24c`（草稿/进行中/提醒）。
- 成功/完成（玉 jade）：`--jade #6f9068`。
- 状态色约定：**jade = 完成**，**gold = 草稿/进行中**，**cinnabar = 主操作/危险提示**。

### 4.2 颜色令牌表（提炼自 `:root`）
| 用途 | 变量 | 值 | 说明 |
|---|---|---|---|
| 页面底色（Chrome） | `--ink` | `#f4ead6` | 暖奶油色（注意：变量名叫 ink 但其实是背景，命名易混淆） |
| 面板最亮面 | `--ink-800` | `#fffdf8` | |
| 面板表面 | `--ink-700` | `#f1e5cf` | 卡片/气泡默认底 |
| 悬停面 | `--ink-600` | `#e8dabf` | |
| 边框 · 弱 | `--line` | `rgba(74,54,38,0.10)` | 棕调半透明 |
| 边框 · 强 | `--line-strong` | `rgba(74,54,38,0.20)` | |
| 正文文字 | `--fg` | `#3b2f26` | 深棕 |
| 次要文字 | `--fg-dim` | `#7b6a59` | |
| 更弱文字 | `--fg-faint` | `#a2917d` | |
| 稿纸底 | `--paper` | `#fbf5e8` | 正文编辑区背景 |
| 稿纸格线 | `--paper-line` | `#e6d9be` | 方格稿纸的网格线 |
| 稿纸文字 | `--paper-ink` | `#3a2e24` | |
| 主色 陶土 | `--cinnabar` / `-bright` / `-deep` | `#c2673f` / `#d67e52` / `#a5502c` | 品牌主色 |
| 成功 玉 | `--jade` | `#6f9068` | |
| 提醒 金 | `--gold` | `#d3a24c` | |

> **设计师须知（缺口）**：
> 1. **没有专门的「错误红」令牌**。错误态目前是散落的 `rgba(197,106,63,…)`（陶土红）或直接写 `#d44` 之类硬编码（见 ExportDialog/TaskQueue）。建议补一套 `--danger` 令牌。
> 2. 代码里出现了 `--ink-900`、`--accent` 两个**未定义**的变量（在 AgentPanel、RoleplayChat 里被引用），浏览器会回退成无效值。属于 bug，改版时应清理或正式定义。
> 3. 正文编辑区「方格稿纸」是招牌视觉元素（`.manuscript` + 绿色网格 + 衬线字体 + 28px 网格对齐），建议保留并作为品牌记忆点。

### 4.3 字体
- 衬线（标题/书卷气）：`--font-serif` = **Noto Serif SC**（中文衬线），英文回退 Songti/SimSun/serif。
- 无衬线（正文/UI）：`--font-sans` = **Noto Sans SC**，回退 PingFang/微软雅黑/system-ui。
- 通过 `app/layout.tsx` 的 Google Fonts `<link>` 加载（需联网首次拉取）。
- 正文基础字号 `15px`、行高 `1.6`；稿纸正文 `17px`/行高 `28px`。

### 4.4 圆角 / 间距 / 阴影
- 圆角：`--radius: 12px`（面板）；按钮 `8px`；胶囊标签 `999px`；印章 `8px`（小号 `6px`）。
- 阴影：仅一套 `--shadow-panel`（面板柔和投影）。
- 间距：代码里大量用内联 `gap: 8/12/14/20`、`padding: 16/18/22` 等，没有统一的 spacing 标尺。建议设计师定义 4/8/12/16/24 间距阶梯，并回填到代码。
- 内容最大宽：`.shell` 最大 `1180px`，左右留白 `24px`。

### 4.5 深浅色主题
- **当前只有浅色（暖奶油）主题，没有深色模式，也没有主题切换按钮/逻辑**（全代码库检索 `dark:`、`prefers-color-scheme`、`theme-toggle` 均无业务代码）。
- 若产品未来要支持深色，需要设计师先定义一套深色令牌，再整体替换 `:root` 变量；目前结构（全部走 CSS 变量）是适合做主题化的。

### 4.6 图标库来源
- **当前并没有使用任何图标库**（代码里查无 lucide-react / react-icons 等依赖；所有「图标」都是：**
  - 文字「印章」徽标 `.seal`（如 `暖`、`阁`、`书`、`拆`、`续`、`章`、`校`）——这是现有唯一的「图标系统」；
  - 少量 Unicode 字符：`✕ ← › ‹ ☰ ＋ ✦ ✓ ▍`。
- **建议**：设计师建立一套统一的图标规范（线框/面性、描边粗细、与印章徽标的搭配规则）。这是明显的体验提升点。

---

## 5. 布局与信息架构

### 5.1 桌面窗口画布尺寸（来自 `electron/main.js`）
> **设计师须知（硬性约束）**：窗口由 Electron 控制，不是自由网页。
> - 默认尺寸：**宽 1280 × 高 860**
> - 最小尺寸：**宽 960 × 高 640**
> - 窗口底色：`#faf6f0`（与页面奶油色接近）
> - 三栏可拖拽：左栏 248px（范围 180–420）、右栏 340px（范围 280–560），折叠后仅留 22px 细条。
> - 设计稿请按 **1280×860** 出图，并考虑 960×640 最小宽度下的可用性问题（窄屏下左右栏会被隐藏，见 globals.css `@media (max-width:900px)`）。

### 5.2 整体外壳（所有页面共享）
```
┌──────────────────────────────────────────────────────────────┐
│ TopBar  暖阁·cozy atelier        [≡ 侧栏]        [助手] 模型档▼│
├──────────┬───────────────────────────────────┬───────────────┤
│ LeftRail │          中工作区 (children)         │  AgentPanel   │
│ 导航                                    │  ┌─────────────┐ │
│  书房首页                                │  │ 助手│角色对话 │ │
│  开一本新书                              │  │─────────────│ │
│  续写                                    │  │             │ │
│  拆书工坊                                │  │  对话/角色   │ │
│  接口设置                                │  │             │ │
│ ────────                                │  │             │ │
│ 书库                                    │  │             │ │
│  《书名》 ●                             │  │             │ │
│  《书名》 ●                             │  └─────────────┘ │
│ ────────                                │                │
│ 任务队列 (固定底部 340px)               │  ← 可折叠为抽屉 │
└──────────┴───────────────────────────────────┴───────────────┘
```

### 5.3 核心界面骨架（3 个）
**① 书房首页 `/`**
```
        [不赶稿·不焦虑 chip]
   找个舒服的姿势，把心里的故事写出来。
   这里是你的暖阁书房……（hero 文案）
   ┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────┐
   │ 书 开新书 │  │ 拆 拆书坊 │  │ 续 续写  │  │ 架 书架  │
   └─────────┘  └─────────┘  └─────────┘  └─────────┘
        （四张入口卡，hover 上浮 + 暖光）
```

**② 作品工作台 `/project/[id]`（主界面）**
```
    ← 书房   《书名》 [改名]  题材chip      保存中…
    [1 立意·大纲]———[2 铺陈·正文]      进度条 12万/100万字 30/120章
   ───────────────────────────────────────────────
   Step1 大纲视图（左右两栏：左侧设定编辑 / 右侧卷·章结构）：
     ┌──────────────┐    ┌──────────────────────────┐
     │ 创作设定       │    │ 卷一《xxx》  [展开章节]     │
     │ 故事圣经       │    │   第1章 脉络…             │
     │ 分卷脉络       │    │   第2章 脉络…             │
     └──────────────┘    └──────────────────────────┘
   Step2 正文视图（左章导航 + 右方格稿纸）：
     ┌────────┐  ┌────────────────────────────┐
     │ 章导航  │  │ [正文|设定库|伏笔] Tab         │
     │ 第1章   │  │ ┌──────────────────────────┐ │
     │ 第2章●  │  │ │  方格稿纸正文（衬线大字）  │ │
     │ …       │  │ └──────────────────────────┘ │
     └────────┘  └────────────────────────────┘
```

**③ 右栏创作助手（AgentPanel）**
```
   [助手] [角色对话]        ✕
   ─────────────────────────
   助手 Tab（AgentChat）：
     写作助手 · 作品id
     气泡流（用户右/陶土，助手左/奶白）
     工具调用 chip（金=调用中/玉=完成）
     ⚠ 待确认改动 提案卡（diff + 确认写入/取消）  ← HITL 确认流
     [技能] [停止/发送]
   角色对话 Tab（RoleplayChat）：
     选角（多选用） → 对话气泡（角色名+内容）
```

---

## 6. 关键交互流（用户路径）

### 路径 ① 写作主流程（大纲 → 正文）
```
书房首页
  → 开一本新书（填书名）→ 进入工作台
  → Step1「立意·大纲」：填创作设定 → 故事圣经(BibleView) → 分卷脉络(VolumeCard/ChapterRow)
       · 任意一步可点「带方向重新生成」→ 出 ChangeSummary 一致性提示
  → 点「铺陈·正文」切到 Step2
  → 在方格稿纸上逐章写作；右侧「助手」可自然语言指令续写
  → 自动保存（防抖 900ms）
```

### 路径 ② 角色对话（找灵感）
```
工作台（已打开某作品）
  → 右栏切「角色对话」Tab
  → 从本书「人物」类设定中勾选角色（可多选）
  → 进入对话：多角色可选「轮转/手动/旁白驱动」模式
  → 角色以第一人称回复，保持人设一致
```

### 路径 ③ 任务队列批量生成
```
左栏底部「任务队列」→ + 新建批量任务
  → 选作品 → 选预设（批量续写 / 批量摘要 / 全卷流水线）
  → 选目标卷 + 起止章号 → 加入队列
  → 轮询状态（每 5 秒）：等待中/执行中/已暂停/已完成/失败
  → 可 开始/暂停/重试/删除
```
> 注：队列面板样式当前是「深色开发者风」（见 §7 痛点 2），与整体暖色严重冲突。

---

## 7. 现有 UI 痛点与改版输入

> 以下为从代码层客观发现的真实问题，已标注「建议保留 / 建议重做」。

### 痛点 1：三个组件用了「深色开发者风」，与暖色语言割裂 ⚠️ 最高优先级
- **现象**：`TaskQueue.tsx`、`HistoryPanel.tsx`、`ExportDialog.tsx` 使用 Tailwind 默认暗色类（`text-neutral-*`、`bg-neutral-900`、`bg-blue-700`、`bg-amber-*`、`text-red-400` 等），呈现黑灰蓝的控制台观感。
- **影响**：任务队列常驻左栏底部，是用户高频可见区；导出弹窗是正式功能入口——观感割裂最严重。
- **建议**：**建议重做**，把这三类统一回「暖阁」令牌（用 `.panel` / `--cinnabar` / `--jade` / 新增 `--danger` 重画）。

### 痛点 2：两个组件已「弃用/未挂载」
- **现象**：`HistoryPanel`、`ExportDialog` 在代码里已写好，但**没有任何页面 import/渲染它们**（检索确认）。版本历史、导出功能当前在界面上实际不可用。
- **建议**：**建议保留组件、补回挂载**——把 ExportDialog 接到工作台「导出」按钮、把 HistoryPanel 接到章节级「版本历史」入口。改版时顺手统一视觉（同痛点 1）。

### 痛点 3：两个超大组件（>1000 行），难维护难改
- **现象**：`StepOutline.tsx` 1039 行、`StepWriting.tsx` 1085 行，UI 与状态逻辑全在一个文件。
- **影响**：设计师改版时定位困难，工程师也易改崩。
- **建议**：**建议保留功能、拆分结构**——StepOutline 已拆出 `step-outline/*` 子组件（做得好，可作范本）；StepWriting 仍建议拆出「章节导航 / 稿纸编辑 / 设定库 / 伏笔 / 提示词」等子模块。改版可借机完成。

### 痛点 4：样式写法不统一（CSS 变量 / 语义类 / 内联 style / Tailwind 四类混杂）
- **现象**：
  - 全局令牌在 `globals.css` 的 `:root`（CSS 变量）；
  - 通用 UI 有语义类（`.btn` `.panel` `.chip` `.seal` …）；
  - 多数功能组件却用**内联 `style={{...}}`** 写样式（AgentChat、RoleplayChat、SkillPicker 等）；
  - 少数组件又用 **Tailwind 工具类**（见痛点 1）。
- **建议**：**建议重做规范**——以「CSS 变量令牌 + 语义类」为准，逐步清退散落内联样式与 Tailwind 暗色类。设计师交付的 Token 表应能直接映射成 `:root` 变量。

### 痛点 5：设计令牌有缺口与错误引用
- **现象**：无 `--danger`（错误红）令牌；`--ink-900`、`--accent` 被引用但未定义（回退无效）；无 spacing/radius 统一标尺；无深色主题。
- **建议**：**建议补齐**：新增 `--danger` 系列、正式定义或删除 `--ink-900`/`--accent`、建立 4/8/12/16/24 间距阶梯。

### 痛点 6：品牌名不一致
- **现象**：产品元数据叫「墨章 · 长篇小说创作工作流」，界面品牌却是「暖阁 / cozy atelier」，设计语言注释也写「暖阁」。用户可能困惑。
- **建议**：**建议保留其一、统一对外**——确认对外品牌是「墨章」还是「暖阁」，全站统一；「暖阁」可作为工作台风别名保留。

### 痛点 7：空状态 / 加载态 / 错误态不系统
- **现象**：部分有空态（书架空、书库空、角色空），但加载态多为文字「加载中…」，错误态散落（`chip--cinnabar` 当错误条、`#d44` 红字、alert 原生弹窗等）。
- **建议**：**建议建立统一规范**：空状态插画/文案风格、加载骨架屏、错误提示条（用新增 `--danger`）。

### 相对健康、建议保留的部分
- ✅ 三栏 IDE 式外壳 + 可拖拽折叠（架构清晰，体验现代）。
- ✅ 「方格稿纸」正文编辑区（强品牌记忆点）。
- ✅ 「印章」徽标系统（`.seal`）作为独特图标语言，可延续并扩展。
- ✅ StepOutline 已良好拆分子组件（可作其它大组件拆分范本）。
- ✅ 状态色语义（jade=完成 / gold=草稿 / cinnabar=主操作）约定清晰，建议升格为正式 Token。

---

## 8. 资产清单

| 资产 | 文件位置 | 用途 / 说明 |
|---|---|---|
| 应用图标（ico） | `build/icon.ico` | Windows 可执行文件图标、窗口图标 |
| 应用图标（png） | `build/icon.png` | 同上，位图版 |
| 品牌印章（文字） | 代码内 `.seal` 渲染（非图片） | 「暖/阁/书/拆/续/章/校」等汉字印章，是主要视觉标识 |
| 字体 | Google Fonts 在线加载（见 `app/layout.tsx`） | Noto Serif SC（标题）、Noto Sans SC（正文）；**无本地字体文件** |
| 全局样式 | `app/globals.css` | 设计令牌、语义类、布局原语、稿纸样式、动画 |
| Tailwind 入口 | `app/globals.css` 顶部 `@import "tailwindcss"` + `postcss.config.mjs` | Tailwind v4（仅作底层工具，令牌未走 Tailwind 主题） |
| 拆书/续写上传 | 用户导入的 `.txt`（运行时，非仓库资产） | 文风卡/设定卡/续写底本 |

> **设计师须知**：项目**没有 `public/` 目录、没有 logo 图片文件**。所有「图标」目前都是文字/Unicode。若改版要引入正式 logo、SVG 图标集、本地字体，需新增资产目录并接入构建（同时建议在 Electron 打包里放本地字体以保证离线）。

---

## 9. 给设计师的技术约束与备注

> 以下为「设计师可自由发挥」与「受架构限制」的边界，请务必在出稿时区分。

### 受架构限制（不可随意改）
- 🔒 **必须保留 Electron 桌面壳**：窗口尺寸、无地址栏、原生保存对话框（`electron/main.js` + `preload.js`）是产品基石。画布按 1280×860（最小 960×640）设计。
- 🔒 **技术栈固定**：Next.js 15 App Router + React 19 + TypeScript + Tailwind v4。改版产出需能被前端工程师落进 React 组件 + `globals.css` 变量体系，**不要给纯 HTML/CSS 切块**（除非约定好映射）。
- 🔒 **Agent 流式交互不可破坏**：右侧「助手」走 `/api/agent/chat` 的 NDJSON 流（`lib/agent/useChat`），含「工具活动 chip + 写操作提案确认(HITL)」闭环。对话气泡、提案卡、流式光标是**功能必需**，重设计时保留这些槽位。
- 🔒 **角色对话依赖数据**：「角色对话」Tab 只能在有作品上下文时用，角色来自本书「人物」类设定（`lib/roleplay/useRoleplay`）。选角→对话的两条状态要保留。
- 🔒 **部分 UI 由 `lib/` 逻辑驱动**：任务队列（批量续写/摘要/全卷流水线）、一致性统一（reconcile）、拆书分析（style/archive）等的状态与文案受后端逻辑约束，视觉可改、信息结构勿删。
- 🔒 **自动保存**：工作台 900ms 防抖保存，UI 上的「保存中/已保存」状态需保留。

### 设计师可自由发挥
- 🎨 视觉风格、配色细化、间距节奏、字体排印、图标系统（建立正式图标规范）、动效（已有 `fadeup`/`pulse` 等，可扩展）。
- 🎨 建立并补全 Design Token（详见 §4.5 缺口），输出一套可映射到 `:root` CSS 变量的令牌。
- 🎨 重做 §7 痛点 1/2 的三个暗色组件、补齐空/加载/错误态、统一内联样式。
- 🎨 桌面端与窄屏（<900px 时左右栏隐藏）的响应式处理。

### 建议交付物形式
1. **Figma 组件库级交付**：以 `globals.css` 现有语义类（btn/panel/chip/seal/card/input）为组件骨架，建立可复用组件，而非逐页切图。
2. **Token 表**：颜色（含新增 `--danger`）、字号阶梯、圆角、间距、阴影、状态色，附 CSS 变量名，方便工程师直接回填。
3. **关键界面高保真**：书房首页、作品工作台（大纲/正文两态）、右栏助手+角色对话、拆书工坊、设置页、导出弹窗、任务队列——并标注窗口尺寸与最小宽度表现。
4. **状态规范**：空状态、加载中、错误提示、HITL 提案确认卡的统一样式。
5. **图标规范**：若引入图标库，给出线框风格、尺寸、与印章徽标的搭配规则。

---

*文档生成依据：实地读取 `components/`（含 `step-outline/`）、`app/`（全部 page/layout）、`lib/constants.ts`、`electron/main.js`、`app/globals.css`、`postcss.config.mjs`。代码行数统计截至生成时。*
